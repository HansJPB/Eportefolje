package edu.ntnu.iir.bidata;

import java.util.ArrayList;
import java.util.List;

/**
 * Creates the AuthorRegister class.
 */
public class AuthorRegister {
  private final List<Author> authorsList;

  /**
   * The method for the AuthorRegister class.
   */
  public AuthorRegister() {
    authorsList = new ArrayList<>();
  }
  public void addAuthor(Author author) {
   //TODO complete this class (use DiaryRegister as help)
    // Turn it to a hashmap so that it can be used as a key (use Diaryregister as data).
  }
}
